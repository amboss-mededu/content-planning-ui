'use client';

import { H1, Stack, Text } from '@amboss/design-system';

// Placeholder landing for the Learning product tab. Content TBD — the tab
// exists so the top-level nav reads as the full AMBOSS product set.
export default function LearningPage() {
  return (
    <Stack space="s">
      <H1>Learning</H1>
      <Text color="secondary">Coming soon.</Text>
    </Stack>
  );
}
