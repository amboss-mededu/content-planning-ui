// Shared row-tint overlays for review surfaces (backlog, articles, sections,
// consolidation, article-manager update views). Applied via `getRowStyle` /
// cell backgrounds to mark rows green (approved) or red (rejected) on top of
// the DataTable zebra stripe.
export const APPROVED_TINT = 'rgba(16, 185, 129, 0.12)';
export const REJECTED_TINT = 'rgba(220, 38, 38, 0.12)';
