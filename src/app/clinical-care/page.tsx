'use client';

import { H1, Stack, Text } from '@amboss/design-system';

// Placeholder landing for the Clinical Care product tab. Content TBD — the tab
// exists so the top-level nav reads as the full AMBOSS product set.
export default function ClinicalCarePage() {
  return (
    <Stack space="s">
      <H1>Clinical Care</H1>
      <Text color="secondary">Coming soon.</Text>
    </Stack>
  );
}
